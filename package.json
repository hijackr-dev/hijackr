{
  "name": "hijackr",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:app": "pnpm --filter @hijackr/app dev",
    "dev:web": "pnpm --filter @hijackr/web dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}